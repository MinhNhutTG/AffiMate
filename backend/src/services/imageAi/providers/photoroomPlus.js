// Provider cho gói PhotoRoom Plus (endpoint /v2/edit, tham số background.prompt) —
// CHƯA triển khai. Cần xác nhận ngân sách nâng gói trước (chuc-nang-tao-anh-ai.md
// mục 11, câu hỏi 10) — hiện chỉ là placeholder để giữ đúng interface chung.
async function generate() {
  throw new Error(
    'photoroom-plus chưa được triển khai — cần xác nhận ngân sách nâng gói PhotoRoom Plus trước khi dùng option "Mô tả nền theo ý bạn".'
  );
}

module.exports = { generate };
